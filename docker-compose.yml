version: '3.8'

services:
  xrp-sentinel:
    build: .
    container_name: xrp-sentinel-v3
    restart: unless-stopped
    volumes:
      - ./data:/app/data
    env_file:
      - .env
    environment:
      - PYTHONUNBUFFERED=1
